import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'GroovoTech - Building Intelligent Digital Experiences with AI & Code',
  description: 'GroovoTech delivers modern AI solutions, mobile apps, web apps, and cloud automation. Transform your ideas into reality with cutting-edge technology.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
